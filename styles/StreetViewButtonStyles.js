import { StyleSheet } from "react-native";

const StreetViewButtonStyles = StyleSheet.create({
  buttonContainer: {
    flexDirection: "column",
    alignItems: "center",
  },
  buttonText: {
    color: "#888",
  },
});

export default StreetViewButtonStyles;
