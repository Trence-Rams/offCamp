import { StyleSheet } from "react-native";

const DistanceInfoStyles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 3,
    paddingVertical: 5,
    width: "100%",
  },
  text: {
    fontSize: 16,
    color: "#333", // adjust the color if needed
  },
});

export default DistanceInfoStyles;
