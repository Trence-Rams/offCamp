import { StyleSheet } from "react-native"

const SignUpScreen_styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'white',
        alignItems: 'center',
      },
})
export default SignUpScreen_styles